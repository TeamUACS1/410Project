<!doctype html>
{% load static %}
<link rel="stylesheet" href="{% static 'css/style.css' %}" media="all" />
<script src="{% static 'js/jquery-1.11.0.min.js' %}"></script>
<script src="{% static 'js/script.js' %}"></script>
<div class="page">
    <div>
	{% for session in sessions %}
    {% if session != "T" %}
        <a class="top" href="main/login"> Login</a>
        <a class="top" href="main/signup"> Sign up</a>
    {% else %}
        <a class="top" href="main/logout"> Log out</a>
    {% endif %}
    </div>
   {%endfor%}
    
    {% block body%} {%endblock%}
    </div>
